name: hello-world
on:
  push:
    branches: ["master", "dev"]
  fork:
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello World!"
      - name: second-step
        run: echo "HI BACK "
